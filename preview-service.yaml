---
apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-preview-svc
  labels:
    app: rollouts-demo-preview
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
      nodePort: 30237
  selector:
    app: rollouts-demo-preview
